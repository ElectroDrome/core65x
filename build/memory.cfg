MEMORY {
    ZP:    start = $0000, size = $0100, type = rw;
    RAM:   start = $0100, size = $1F00, type = rw;
    CODE:  start = $8000, size = $7FFA, type = ro, file = %O;
    VECTORS: start = $FFFA, size = $0006, type = ro, file = %O;
}